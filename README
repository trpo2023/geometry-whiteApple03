# Чтобы собрать проект выполните следующие команды:
cd geometry-whiteApple03
make

# Описание make команд:
make - собирает проект

make run [params=""] - запускает проект

в params передаются параметры для исполняемого файла
возможные параметры:
-d [путь до файла] - программы будет считывать данные не из стандартного потока ввода, а из файла. 
если не указывать путь до файла, то считывание будет из файла geometry-whiteApple03/input/input

фигуры, в которых была обнаружена ошибка, выводятся на стандартный поток ввода
остальные фигуры записываются в файл geometry-whiteApple03/output

make clean - удаляет все объектные и исполняемые файлы

make debug - создает файл для отладки программы