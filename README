# Чтобы собрать проект выполните следующие команды:
cd ./src
make

# Описание make команд:
make - собирает проект

make run - запускает проект

make clean - удаляет все объектные и исполняемые файлы

make debug - создает файл для отладки программы